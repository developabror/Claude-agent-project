#!/usr/bin/env bash
# SessionStart — announce that this is the monorepo root (orchestration only).
echo "🧭 monorepo ROOT — orchestration + shared skills loaded. Backend/frontend specialist agents are NOT loaded here (cd into a side for a focused, isolated team). Root commands: /build-zero-to-prod · /new-feature · /contract-sync · /ship · /status."
exit 0
