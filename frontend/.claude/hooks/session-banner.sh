#!/usr/bin/env bash
# SessionStart — announce the active team for a frontend session.
echo "⚛️  FRONTEND session — React/TS/Vite team loaded. Backend (Spring Boot) agents are NOT in scope. Commands: /scaffold-frontend · /new-component · /new-page · /wire-api. Read CLAUDE.md for the delegation map."
exit 0
