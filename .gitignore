# ── Claude Code ──────────────────────────────────────────────
# Personal, machine-local Claude settings & state (never commit)
.claude/settings.local.json
**/.claude/settings.local.json
.claude/.last-cleanup
.claude/plans/
.claude/file-history/
CLAUDE.local.md
**/CLAUDE.local.md

# ── Prompt-base (root-generated implementation prompts — LOCAL ONLY) ──
# A root session writes hand-off prompts here for you to apply from inside each side.
# Never commit these. If already tracked: git rm -r --cached backend/prompt-base frontend/prompt-base
prompt-base/
**/prompt-base/

# ── Secrets / env ────────────────────────────────────────────
.env
.env.*
!.env.example
*.pem
*.key
secrets/

# ── Backend (Gradle / Java) ──────────────────────────────────
backend/.gradle/
backend/build/
backend/**/build/
backend/out/
backend/bin/
*.class

# ── Frontend (Node / Vite) ───────────────────────────────────
frontend/node_modules/
frontend/dist/
frontend/coverage/
frontend/.vite/
frontend/playwright-report/
frontend/test-results/

# ── IDE / OS ─────────────────────────────────────────────────
.idea/
.vscode/
*.iml
.DS_Store
Thumbs.db
