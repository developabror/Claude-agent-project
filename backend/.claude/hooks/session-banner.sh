#!/usr/bin/env bash
# SessionStart — announce the active team for a backend session.
echo "☕ BACKEND session — Spring Boot/Java 21 team loaded. Frontend (React) agents are NOT in scope. Build with ./gradlew (NOT Maven). Commands: /scaffold-backend · /new-endpoint · /new-entity · /add-migration. Read CLAUDE.md for the delegation map."
exit 0
